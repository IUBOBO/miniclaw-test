# 本项目协作约定

先阅读 CLAUDE.md，其中的架构、权限、测试和数据保护约束继续适用。

## 固定工作目录

本项目正式开发与运行目录是 E:/Agent/miniclaw-test。不要修改旧的 E:/AgentStudy 下的参考源码或 yb_test 副本来实现本项目功能。

## 每次修改必须更新进度

用户要求随实际进度持续维护版本。每完成并验证一个独立功能或修复，更新 docs/项目进度与修改记录.md：

1. 写明本次版本、日期、变更原因、修改文件及实际验证结果。
2. 更新功能表状态与剩余问题，注明用户还需要提供哪些材料。
3. 功能阶段依次使用 V0.1、V0.2 等；同一阶段的小修复使用 V0.1.1。未验收的工作只记进行中，不提前标完成。
4. 同步 LOCAL_VERSION。版本号表示本地项目进度，不修改上游产品版本伪装贡献。
5. 真实模型、真实科研场景、生产效果分别验收。源码复用、Mock、自测通过不能代替真实调用或科研评测。

## 启动与数据

使用根目录 start.cmd 或 start.ps1。禁止重建或删除 data 来解决普通启动问题。不要把 API Key、数据库、日志、研究数据提交到 Git。pnpm-lock.yaml 是本地启动脚本使用的依赖锁；保留原始 package-lock.json 作为上游参考，修改依赖时同步对应 pnpm 锁。

每次只停止已确认属于本项目的监听进程，不能按端口模糊批量终止其他服务。
