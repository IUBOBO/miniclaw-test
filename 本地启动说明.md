# MiniClaw 本地启动说明

正式开发与运行目录：E:\Agent\miniclaw-test。所有新代码在此修改。原始参考位于 E:\AgentStudy\miniclaw-agent\miniclaw-main\miniclaw-main，本次未复制旧 yb_test、数据库、账号或密钥。

## 每次启动只需这一条命令

在 PowerShell 或“运行”窗口中执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1"
```

也可双击 start.cmd。默认地址 http://127.0.0.1:3310 。服务后台运行，启动命令结束或关闭该窗口不会主动停止服务；没有配置开机自启。

脚本检查三个包的依赖和构建：后台、网页、Agent Runner。首次或依赖变化时安装；源码改变时构建；未改变时复用构建。已运行且源码未变时直接返回地址；端口被其他服务占用时报告错误，不擅自杀进程。修改源码后重新执行同一启动命令即可构建并重启本项目。

## 停止 状态与重新构建

```powershell
# 状态
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1" -Action Status

# 停止本项目，保留 data
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1" -Action Stop

# 强制重建并启动
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1" -Rebuild

# 依赖文件修改后重新安装并启动
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1" -Install

# 只构建，不启动
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1" -Action Build

# 端口冲突时选择其他端口
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\Agent\miniclaw-test\start.ps1" -Port 3311
```

停止会终止本项目进程树；先结束正在执行的任务。它保留数据库，但不是任务完成或训练保存操作。不提供删除数据的 Rebuild 功能。

## 环境与配置

- 当前已安装并验证 Node.js 22.23.2、npm 10.9.8、Git for Windows 2.55.0（含 `C:\Program Files\Git\bin\bash.exe`）和 uv 0.12.16。Runner 在 Windows 使用 `where.exe` 棖测 Node/uv；换电脑后需重新安装这些工具。
- 启动脚本优先使用 PATH 中的 Node/pnpm，找不到时才使用当前用户 Codex 提供的 Node/pnpm。源码与数据不依赖旧 yb_test。
- 本次依赖安装复用了旧目录的 pnpm 下载缓存。运行时使用新目录 node_modules；缓存不是源码或运行数据。脚本可识别现有缓存；删除旧缓存后，下一次 -Install 会使用本项目 .local/pnpm-store 重建依赖，需联网。
- 正常重复启动不下载依赖。缺失依赖或重新安装时需访问包仓库。
- 默认仅监听 127.0.0.1。后台新增 WEB_HOST 环境变量支持；启动脚本设为本机访问。
- 不注入固定测试密钥，服务自动生成并持久化 data/config/session-secret.key；不要公开该文件。
- 本次跳过 Electron 和 Playwright 浏览器下载，因为交付是 Web 版本。Runner 的 agent-browser 安装检测到本机 Chrome；浏览器工具尚未进行完整验证。

## 第一次使用

1. 打开工作台，创建管理员账号；此处未替你设置用户名或密码。
2. 在模型设置中填写服务地址、密钥和实际模型名称，给 Agent 选择模型配置。
3. 自定义 Provider 可选择 Anthropic Compatible 或 OpenAI Compatible；每种协议独立保存 Base URL。协议、地址、模型名称必须与服务商实际接口一致。
4. 本地先用 active 管理员所属的 Host 执行方式。Host 直接在电脑上执行工具，不是容器沙箱。非管理员 Host 执行会被拒绝。
5. 首先验证一条真实对话，再验证读取指定工作区文件；这两步完成后才记“真实模型链路通过”。
6. 只在需要容器执行时安装 Docker 并准备 Agent 镜像；消息渠道可稍后配置。

## 数据 日志与备份

- data/：数据库、模型配置、会话与工作区资料；更换版本时保留。
- logs/service-时间.out.log、.err.log：每次启动独立日志，不覆盖上次崩溃记录。
- .local/service.json：本项目进程信息；.local/build.json：构建指纹。
- docs/项目进度与修改记录.md：最新进度、功能清单和材料清单。
- LOCAL_VERSION：本地里程碑版本。

备份时先停止服务，再复制完整 data/ 到你自己的备份目录。首次迁移没有导入旧账号或聊天；如需迁移，应单独安排备份和数据库检查，不能运行中直接覆盖。

如果页面打不开，先执行 -Action Status，再查看最新 err.log。旧副本曾发生 Node 原生模块退出断言；本次短时验证不等于长期稳定性证明，不用反复清空数据库处理崩溃。

## V0.1.1 数据库兼容性修复

本地服务要求 Node.js 22+（当前系统安装并用于服务进程的是 22.23.2），better-sqlite3 固定为 13.0.3。继续使用原 start.ps1 命令即可安装依赖和构建。pnpm-lock.yaml 为本地锁文件；package-lock.json 仍是上游参考，不能用于还原本次本地修复。

隔离账号回归：在项目目录执行 `node scripts/check-local-auth.mjs`。脚本使用临时目录、随机测试密码和系统分配端口，测试创建账号、20 次登录及 90 秒状态查询；不会操作正式账号。结果写入 logs/auth-regression.json。失败日志保留在系统临时目录。
