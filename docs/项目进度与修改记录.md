# MiniClaw 科研服务项目进度与修改记录

当前版本：V0.6.0（LOCAL_VERSION 为 0.6.0）。正式开发与运行目录：E:/Agent/miniclaw-test。

最新已验证状态：完整源码、前后端与 Runner 已就绪；真实 OpenAI Compatible 模型对话与 Workspace 文件读取已经通过。科研材料已迁入 E:/Agent/miniclaw-test/miniclaw-data。Research Workspace Loader、research_workspace_status、research_paper_search、research_code_lookup 与 research_paper_analyze 均已实现；前四项完成显式工具验收，论文分析完成自然语言自动选工具验收，正式版本为 V0.6.0。用户可直接分析工作区内只有 PDF 的新论文，获得研究问题、方法、实验、结论、页码来源和当前可验证边界。

目标：面向高校深度学习科研，将论文、代码、实验配置和训练日志串联为可追溯、可接续的研究任务，并以真实遥感图像分割项目验收。

## 需要完成的事情

| 阶段 | 要做的事情 | 具体功能或工作 | 完成标准 | 当前状态 | 完成记录 |
|---|---|---|---|---|---|
| P0 | 统一开发位置 | 将完整源码复制到 E:/Agent/miniclaw-test，保留原始基线和 Git，开发、构建和启动统一使用此目录 | 修改该目录后，启动的就是修改后的版本 | 已完成 | 2026-09-17，V0.1；新目录源码、Git 基线、start.ps1；详见下方历史 |
| P0 | 完整启动 | 安装前后端及 Agent Runner 依赖，构建三部分；提供固定启动脚本、日志位置和停止方式 | 一个命令启动；首页与健康检查正常，Runner 构建与模块加载通过；真实调用另验收 | 已完成 | 2026-09-17 V0.1 完成一键启动；2026-09-18 V0.2.1 补齐 Node.js 22、Git Bash、uv，修复 Windows 能力预检并重启验收 |
| P0 | 模型接入 | 初始化管理员，配置 API 地址、密钥、模型名称和对应 Agent；选择执行模式 | 完成一次真实对话和一次工作区文件读取，保存脱敏调用记录 | 已完成 | 2026-09-18 V0.2：完成真实 OpenAI Compatible Provider 调用验收；第三方模型可正常回复。随后在独立 BDGI-Research-Test Workspace 中创建真实 Markdown 文件，Agent 通过 Pi Runtime 实际调用 Read/Grep 工具读取文件并基于工具结果正确回答；Session JSONL 已记录 toolCall 与 toolResult。真实对话与 Workspace 文件读取两项 P0 验收均通过。 |
| P0 | 稳定性 | 复现并定位 Node 原生模块断言错误，检查运行时与依赖兼容性，保留错误日志 | 连续使用、重复调用和重启验证中不再出现同类崩溃，并记录测试时长、操作及结果 | 部分完成，持续观察 | 2026-09-18 V0.1.1：升级原生数据库依赖并通过隔离登录回归；未完成长期稳定性验收 |
| P1 | 资料与证据模型 | 为论文、代码、配置和日志建立统一标识，记录路径、版本、页码或行号、文件哈希；建立实验关联 | 每个结论都能找到对应材料及版本，代码、配置、日志可关联到同一实验 | 进行中：读取与校验基础已实现 | 2026-09-19：Loader 已读取工作区、manifest、论文及 Prediction Run，并返回快照和完整性状态；Finding、ResearchQuestion、Todo 与结论引用尚未实现 |
| P1 | Tool Calling | 实现论文/笔记检索、仓库搜索、配置比较、日志解析四类科研只读工具；统一参数校验、路径边界、输出限制和错误返回 | Agent 能实际调用四类工具，结果可人工核对；非法输入、越界与截断有明确处理 | 进行中：状态、论文检索与代码定位已通过客户端验收 | research_workspace_status、research_paper_search 与 research_code_lookup 均已完成自动测试、真实材料检查和客户端 Tool Calling；配置比较和日志解析仍未实现 |
| P1 | Context Engineering | 按“问题→论文→代码→配置→日志→结论”组织跨工具任务；按需加载证据，控制上下文预算，区分事实、推断、冲突与未知 | 输出带来源的研究说明；保留矛盾证据，材料不足时明确指出缺口 | 未开始 | — |
| P1 | Runtime | 复用 Pi Runtime，增加科研任务与工具调用状态、耗时、错误、超时及取消记录；支持重新发起失败任务 | 成功、失败、取消均有可核对记录，不错误显示成功；重跑能追溯前次记录，不声称恢复进程现场 | 未开始 | — |
| P1 | Memory | 复用 SQLite/FTS5，关联研究问题、证据、实验与待办；保存修订，检查来源变化和更新冲突 | 新会话能继续任务；过期证据不被当作当前事实，旧版本更新不会覆盖新记录 | 未开始 | — |
| P1 | Agent Eval | 准备正常、缺材料、错误配置、日志不匹配、证据冲突、越界、超时、接续等用例；区分工具测试与真实任务评测 | 有固定输入、预期行为、实际结果及失败原因；修复后可执行回归 | 未开始 | — |
| P2 | 产品使用 | 完善科研工作台，支持选择研究任务、查看材料与执行进度、浏览证据、导出研究报告 | 能通过工作台完成一次完整科研任务，输出及证据可查看、可导出 | 未开始 | — |
| P2 | 项目交付 | 整理 README、架构图、提交记录、测试报告、演示流程和已知限制，建立功能与简历主张对应关系 | 能明确解释“基座已有什么、我新增什么、如何验证”，每条完成式主张有证据 | 部分完成 | 已有启动说明、材料规范和 CONTRIBUTIONS.yaml；科研功能实现、架构图、任务级测试及演示仍待完成 |

## 需要准备的材料

| 材料 | 最低需要提供什么 | 用于什么 | 准备状态 | 保存位置或备注 |
|---|---|---|---|---|
| 模型服务信息 | 可用的 API 地址、模型名称、密钥及接口类型；密钥只填入本地配置 | 跑通真实 Agent | 已配置并验证 | 2026-09-18 已完成真实 OpenAI Compatible 调用；密钥仅保存在本地配置，本表不记录密钥或密码 |
| 遥感分割代码仓库 | 一份实际使用的仓库，固定 commit 或源码快照，注明训练与评测入口 | 代码定位、版本关联、真实场景验证 | 已提供固定快照 | E:/Agent/miniclaw-test/miniclaw-data；入口 root 改为相对路径；采用 SNAP-20260919-92afd6116c4d 与 snapshot-manifest.jsonl 固定版本 |
| 论文与阅读笔记 | 与仓库或研究问题相关的论文正文、来源及笔记；优先可提取文字的 PDF | 检索、证据定位、方法对照 | BDGI 主论文与方法映射已完成 | PDFpaper/BDGI-Net.pdf 已纳入 manifest；PAPER-BDGI.yaml 记录在投状态、哈希和表格页码；notes/methods/BDGI-Net.md 完成论文—源码映射 |
| 明确的研究问题 | 先选一个问题，例如“某模块在代码哪里实现，其实验结果是否支持论文结论？” | 限定首个端到端任务 | 已确定题目，待接入 Agent | 首题固定为“GMRM 在论文中如何描述、在源码哪里定义和调用”；人工证据位于 PAPER-BDGI.yaml 与 notes/methods/BDGI-Net.md |
| 实验配置 | 基线和一组变更配置，包含模型、训练参数、数据划分及随机种子等实际信息 | 配置比较与实验条件核对 | 已提供，作为工具测试材料 | 共 81 份 YAML；后续配置工具必须报告 batch size、路径等混杂差异。本轮不要求用配置重新复现论文指标 |
| 训练与评测日志 | 对应上述实验的原始日志，注明代码版本、配置和运行时间 | 指标解析、失败分析、结果追溯 | 已提供日志，论文指标另有权威来源 | 75 份 LOG、234 份 CSV 与事件文件用于日志解析测试；最终性能只读取 PAPER-BDGI-RESULTS.yaml，不要求日志复现论文表格 |
| 数据说明 | 数据集名称、目录结构、类别、训练/验证/测试划分；必要时提供可使用的小样本 | 判断实验是否具有可比性 | 掩码核验材料已完成 | SD 73 组、SC 20 组 input—ground truth—saved prediction 已复制并逐文件校验；数据集规模以论文表 1 为准，历史划分身份不再作为缺口 |
| 指标定义 | 实际采用的 mIoU、F1、Dice 等指标的计算方式、类别平均方式和评测脚本位置 | 避免比较不同统计口径 | 已完成 | METRIC-PAPER、METRIC-PIXEL-MASK、METRIC-OBJECT-MASK 分开记录；mask_ARF.py 为逐图像素指标，mask_eval.py 为连通域对象指标 |
| 人工核对答案 | 对首批研究问题给出正确代码位置、配置差异、日志数值和允许得出的结论 | 建立评测基准 | 核心映射已具备，正式 Eval 待建立 | notes/methods/BDGI-Net.md 给出 GMRM/ETA/TAB 等正确代码位置与结论边界；后续转成 Agent Eval 固定用例 |
| 环境说明 | 操作系统、Python/PyTorch 等版本、依赖文件；若涉及实验运行，再补 GPU/CUDA 信息 | 核对运行条件 | MiniClaw Host 已具备；分割训练环境不在本轮范围 | Node.js 22.23.2、Git/Bash、uv 已验证。本轮不运行训练，因此 Python/PyTorch/CUDA 训练环境不作为材料缺口 |
| 个人开发记录 | 实践过程中保存提交、diff、新增文件和修改说明 | 证明个人贡献 | 部分具备 | 已有 Ownership 元数据；个人新增 Research Workspace Loader、research_workspace_status、research_paper_search、research_code_lookup、路径/哈希校验及专项测试；配置比较和日志解析仍待实现 |
| 验证记录 | 工具调用轨迹、报告、跨会话恢复记录、测试输出及失败案例 | 支撑简历完成式主张 | 状态、论文检索与代码定位客户端调用已验收 | 三个科研工具的客户端 Session 均已核对；代码定位两次 Tool Result 返回 model.py 第 332、443、500 行及一致的 snapshot/SHA-256；29 项相关测试与已注册 MCP handler 直接调用通过 |

## 完成一项推进一项

- 每完成一项先验收，再更新对应行的状态、日期、实际版本和证据位置；未通过验收不标为已完成。
- 部分完成写清已做内容与剩余工作；材料收到但未核对时写“已收到，待核对”。
- 每次记录本次完成什么、如何验证、下一步做什么；保留历史，不覆盖原有验收结果。
- 功能阶段使用 V0.1 → V0.2 → V0.3，同阶段小修复使用 V0.1.1，并同步 LOCAL_VERSION。未来版本是路线，不是已经实现的能力。
- 单纯整理文档不提升功能版本；本次因修复 Windows Runner 能力预检并补齐 Host 工具环境，版本推进为 V0.2.1 / 0.2.1。
- 基座构建、Mock 与单元测试、真实模型调用、科研任务评测分别记录，不互相替代。密钥和密码不写入本文件或 Git。

## 修改记录

### 2026-09-17 清单文档整理

- 本次完成：按用户提供的格式整理 12 项功能和 12 类材料，补齐完整工作内容、完成标准、状态及记录位置；采用迁移与 Runner 已验证的最新状态。
- 验证方式：核对两表行数与必备事项、保留原 V0.1 历史及验证内容、检查 LOCAL_VERSION 不变，并运行项目文档一致性检查。
- 下一步：初始化管理员并配置兼容模型服务，验收真实对话与工作区文件读取；同步准备一个分割仓库、相关论文、基线/变更配置及匹配日志。
- 版本处理：仅调整文档，维持 V0.1，不将任何科研功能提前标记完成。

## V0.1 迁移与启动基线

日期：2026-09-17。

- 完整源码迁移到正式目录，保留上游结构与许可，未复制旧 yb_test 运行数据。
- 新增 start.ps1、start.cmd、scripts/local-service.mjs：依赖检查、三包构建、后台启动、重复启动检查、停止、状态和日志。
- src/web.ts 增加 WEB_HOST，启动脚本使用本机监听。
- 补齐后台 axios、@hapi/boom，网页 katex，Runner 的 @earendil-works/pi-ai 直接依赖；生成本地 pnpm 锁文件。
- 新增 AGENTS.md 维护规则、启动说明和本进度表。
- 未改变 Pi 执行引擎、业务权限规则或新增科研工具。

### 本次验证

验收日期：2026-09-17。机器记录位于 logs/local-start-verification.json（运行日志不进入 Git）。

| 检查 | 结果 |
|---|---|
| 后台 TypeScript 构建 | 通过 |
| 网页 TypeScript 与 Vite 构建 | 通过；仍有上游大 chunk 提示，不阻止运行 |
| Agent Runner TypeScript 构建 | 通过 |
| Runner PiRuntimeAdapter 模块导入 | 通过；未调用模型 |
| 首页与 /api/health | 均为 HTTP 200 |
| 监听地址 | 127.0.0.1:3310，仅本机 |
| 重复启动 | 识别现有本项目进程，PID 不变 |
| 停止后再次启动 | 通过，数据目录保留 |
| 其他服务占用端口 | 拒绝启动，不误杀其他进程 |
| agent-runtime-migration、agent-runner-prompt-plan、routes-workspace-memory | 3 个文件、23 项测试通过 |
| 文档一致性检查 | 通过 |
| 管理员初始化 | 尚未完成，未替用户创建账号 |
| 真实模型与科研场景 | 未执行；不能视为已通过 |

本次修复了 Windows 启动脚本参数引用，以及原清单缺少直接依赖造成的构建问题。依赖安装曾出现下载超时，重试后成功；不代表所有网络环境都能离线初装。测试使用已安装的 node_modules/vitest/vitest.mjs 直接执行，避免包管理器自动换缓存重装。

旧副本 Node 原生清理断言的确切根因仍未确认。新项目的构建、短时运行与上述测试通过，但不能据此宣称长期崩溃风险已消除。

## 最小科研验收任务

选一个固定版本仓库，输入真实研究问题，检索论文，定位代码，比较配置，读取已有日志，输出带来源的结论和待办；关闭会话后正确接续。缺资料或口径不一致时明确未知，不能伪造结论。

## 个人贡献与基座边界

自托管框架、Pi Runtime、原有工具框架、SQLite/FTS5 Memory 均来自开源基座。科研工具、研究关联层、Trace 和任务评测才是后续新增目标。只完成配置不等于自研底层运行时；只完成此文档不等于完成科研平台。

## 下次从哪里继续

V0.3：先提供一个固定版本的遥感分割仓库、一篇相关论文、基线与变更配置、对应训练日志和一个明确研究问题，再实现统一证据记录与首个只读科研工具。真实模型与 Workspace 文件读取已经完成，不再重复作为下一阶段任务。

科研阶段开始前先提供仓库、论文、基线/变更配置和匹配日志。后续每完成一个功能，将本文件新增相应版本小节，保留 V0.1 历史，不覆盖原有验证记录。

## 2026-09-18 初始化 Network error 排查（功能版本不变）

- 新目录服务启动后也发生原生断言崩溃，网页提交账号因此连接失败。错误证据：logs/service-2026-09-17T16-17-52-235Z.err.log；包含 RemoveEnvironmentCleanupHook、env != nullptr 和 Statement 析构堆栈。根因仍未确认，P0 稳定性处于排查中，不能标为完成。
- 已通过原 start.ps1 重启，未删除数据库、未创建或修改账号、未修改模型配置。新日志：logs/service-2026-09-17T16-22-05-228Z.out.log 及同名 err.log。
- 连续 200 次 /api/auth/status 查询通过，返回 initialized=false。独立内存库的同步、异步显式垃圾回收及 100 万次语句自动回收检查通过，尚未稳定复现崩溃。
- 此次仅恢复服务及记录排查结果，不提升 LOCAL_VERSION，不代表管理员提交、模型调用或长期稳定性验收通过。
- 下一步：网页重试管理员初始化；若失败，根据最新日志定位请求与崩溃关系，继续核验 Node 与原生模块兼容性。

## V0.1.1 — 2026-09-18 原生数据库依赖升级与登录回归

- 原因：新副本再次发生 RemoveEnvironmentCleanupHook 断言，后台退出使登录页面显示 Network error。旧版 better-sqlite3 与当前 Node 组合存在兼容性嫌疑；未获得可稳定复现的最小用例，不将推断写成完整根因证明。
- 修改：package.json 和 pnpm-lock.yaml 锁定 better-sqlite3 13.0.3；start.ps1 与 scripts/local-service.mjs 明确 Node.js 22+；新增 scripts/check-local-auth.mjs 隔离 HTTP 回归；tests/db-transactions.test.ts 在清理临时库前关闭主连接；本地启动说明同步更新。
- 验证：前后端与 Runner 构建通过；重点数据库回归 4 文件、25 测试通过；隔离临时库创建账号成功（201）、20 次登录成功（200）、90 秒内 90 次状态查询成功。证据 logs/auth-regression.json。正式服务 health 正常、initialized=true，未改动用户密码或删除数据。
- 全量测试限制：303 文件通过、31 文件失败；2803 测试通过、50 失败、23 跳过。存在 Windows 路径、符号链接权限、POSIX 权限位及文案等失败，未逐项完成基线归因，不宣称全量测试通过。详细输出 logs/v0.1.1-tests.log。
- 上游依据：https://github.com/WiseLibs/better-sqlite3/releases/tag/v13.0.0 （改用 N-API）；同堆栈案例 https://github.com/tobi/qmd/issues/827 。外部案例只作为修复方向，不替代本项目验证。
- 剩余：用户浏览器登录、长时间使用和重启后观察；模型服务配置及真实调用仍待完成。原一键启动命令保持不变。本次没有提交或推送 GitHub。
## 2026-09-18 文档补充：技术栈与源码导航

新增 docs/技术栈与核心代码修改清单.md，按实际 package.json、源码入口和项目协作约定整理技术栈、核心目录/文件、科研功能修改位置与验证顺序。建议新增路径明确标注未实现。同步指出 Node engines 与启动要求不一致、数据库依赖为范围声明且通过锁文件解析。仅整理文档，LOCAL_VERSION 保持 0.1.1，未修改功能源码或模型配置。

## V0.2 开发中 — 2026-09-18 OpenAI-compatible Provider 协议接入

- 目标：在保留原有 Anthropic Compatible Provider 的基础上，为第三方模型服务增加 OpenAI Compatible 协议选择，不将模型协议写死为 Anthropic Messages。
- Runner 改造：新增 `anthropic-messages` 与 `openai-completions` 两种 Provider API 协议，并将协议信息沿 `provider-runtime -> RuntimeSessionOptions -> PiRuntimeAdapter -> resolvePiProvider -> ModelRuntime.registerProvider` 传递。
- Provider 配置：后端 Provider 数据结构新增 `apiProtocol`，旧配置缺少该字段时默认回退到 `anthropic-messages`，保持向后兼容。
- 环境注入：新增 `MINICLAW_PROVIDER_API`，Provider 配置会在启动 Agent Runner 时注入对应协议，避免由自定义环境变量覆盖系统协议选择。
- API 层：Provider 创建和修改 Schema 支持 `apiProtocol`；修改协议时视为运行时协议级变更，并使相关旧 Session 失效，避免旧协议状态与新 Provider 配置混用。
- 前端：第三方模型配置页面新增 `Anthropic Compatible` / `OpenAI Compatible` 选择；OpenAI Compatible 模式提示使用 OpenAI Chat Completions 兼容 Base URL。
- 兼容性：Claude 官方 Provider 固定使用 `anthropic-messages`；已有第三方 Provider 未配置协议时继续使用 Anthropic Compatible。
- 验证：执行 `powershell -ExecutionPolicy Bypass -File .\start.ps1 -Action Build`，Backend、Web 与 Agent Runner 均构建通过；前端协议切换界面已人工确认正常。
- 当前限制：尚未使用真实 OpenAI-compatible API 完成模型请求，因此不能将 V0.2 标记为完成；Workspace 文件读取链路也尚未完成真实验收。
- 下一步：配置真实第三方 OpenAI-compatible API，完成一次真实对话；随后绑定 Workspace，通过 Agent 实际读取 Workspace 文件，并保存脱敏验证结果。

## V0.1.3 — 2026-09-18 双协议独立地址与随时切换

- 本次完成：Provider 编辑器支持 Anthropic Compatible / OpenAI Compatible 来回切换，分别记住和修改 Base URL；切换到未配置的协议时显示空地址，要求填写后保存。保存后按当前协议和地址运行，切回时恢复此前保存的地址。
- 配置与兼容：新增 protocolBaseUrls 映射，旧配置的地址自动归入其原协议；保留原有字段作为当前生效地址，密钥仍加密保存。模型、密钥及高级参数按 Provider 共用，两个接口需要不同凭据或模型时建立两条 Provider。官方 Claude 固定使用 Anthropic。
- 修改文件：src/runtime-config.ts、src/schemas.ts、src/routes/config.ts、web/src/components/settings/ProviderEditor.tsx、web/src/components/settings/types.ts，以及两份 Provider 回归测试。
- 会话行为：修改当前地址或协议会使绑定的旧会话失效；未填写目标协议地址时拒绝切换，不沿用另一协议地址。
- 验证：4 个测试文件、43 项测试通过，覆盖旧数据兼容、地址持久化、双向切换、运行环境注入和会话失效；Backend、Web、Runner 三部分构建通过。修复测试在 Windows 清理临时目录前未关闭数据库的问题。
- 边界：尚未以真实模型完成两种协议的成功对话与 Workspace 文件读取；科研功能仍未开始，不提升 P0 模型接入为已完成。
- 操作说明：docs/Provider协议切换使用说明.md。下一步先用所选协议发送一条新消息，再验收工作区文件读取。

## V0.1.2 — 2026-09-18 OpenAI-compatible 消息无回复修复

- 现象：Web 页面能保存并显示用户消息，但 Agent 长时间不返回结果。
- 已确认原因：OpenAI-compatible 请求已经到达上游；`deepseek-v4.1-flash` 会话实际使用 `medium` 推理档位，而上游只接受 `low/high/xhigh/max` 或规定范围整数，并要求参数位于 `chat_template_kwargs`。请求因此返回 HTTP 400。早期还出现过 Base URL/协议不匹配导致的 404；切回 Anthropic 后出现 `Anthropic stream ended without a stop reason`。
- 体验问题：这些确定性错误沿通用重试路径静默处理，Web 端收不到错误消息；失败的 Pi turn 还可能等待后续输入，放大了“无回复”的表现。
- 代码修改：实际 `pi-index.ts` 接入 Agent/Provider 推理档位；为已验证的 `api.icompify.com + DeepSeek V4.1 + OpenAI Chat Completions` 组合增加 endpoint-scoped `chat_template_kwargs.reasoning_effort` 映射；失败 turn 立即结束；Host 输出解析器保留 Runner 的结构化错误；400、404 和协议流格式错误向 Web 返回脱敏说明并跳过无效重试。
- 构建修改：Host Runner 的过期检查改为递归检查全部 TypeScript 源码，并使用当前 Node 直接运行项目内 TypeScript 编译器，不再依赖系统 `npm`，避免自动编译失败后继续使用旧 dist。
- 验证：Runner 编译与后端类型检查通过；Backend、Web、Runner 三部分重新构建并启动，`/api/health` 返回 healthy；最新聚焦回归 4 个测试文件、60 项测试通过，覆盖协议解析、推理档位传递、网关参数映射、失败 turn 收口、结构化错误透传、错误提示和 Provider 失败策略。
- 运行验证：服务恢复旧待处理消息时，Runner 返回 `Anthropic stream ended without a stop reason`；Host 保留原始错误，后台记录 `Provider request rejected; skipping retry`，当前服务日志中没有新的 `Scheduling retry` 或静默重试记录。该结果证明错误收口生效，不代表 Anthropic 配置可正常对话。
- 配置复核：2026-09-18 17:49 的失败会话仍显示 `provider: miniclaw-anthropic`、`api: anthropic-messages`；持久配置实际为 `anthropic-messages + https://api.icompify.com/v1`，因此 Anthropic 请求打到 OpenAI 地址并返回 404。审计记录显示当次只改变了 Base URL，`apiProtocolChanged=false`。17:57 已保留原密钥、地址和模型，仅把当前 Provider 协议纠正为 `openai-completions` 并重启；健康检查通过。
- 验证边界：纠正后的 OpenAI-compatible 配置尚未通过新的网页真实对话验收；之后还需验收 Workspace 文件读取。项目自带 `sdkQuery` 冒烟脚本固定走 Anthropic 链路，不能用来替代本次 Pi/OpenAI-compatible 路径验证。
- 详细说明：`docs/V0.1.2-OpenAI协议无回复修复说明.md`。

## V0.2 — 2026-09-18 真实模型与 Workspace 工具链验收完成

- 真实模型：使用第三方 OpenAI Compatible Provider 完成实际模型调用并获得正常回复。
- 协议链路：`apiProtocol=openai-completions` 已从 Provider 配置传递至 Pi Runtime，并完成真实请求验证。
- Workspace：创建独立 `BDGI-Research-Test` 工作区，实际目录隔离于 Main Workspace。
- 文件读取：在 Workspace 中创建真实 Markdown 文件，Agent 能识别并读取实际文件内容。
- Tool Calling：Pi Session JSONL 中确认存在 `read` Tool Call，并记录对应工具结果；同时可见 `grep` 工具调用结果。
- 回答验证：Agent 最终输出与本地文件原始内容一致，并能准确识别当前 Workspace 路径与文件数量。
- 验收结论：真实模型对话与 Workspace 文件读取均通过，P0 模型接入完成。
- 下一步：进入 V0.3 Research Workspace，开始接入真实遥感深度学习科研材料，并开发论文、代码、实验配置和训练日志的科研 Tool Calling 能力。

## V0.2.1 — 2026-09-18 Host 科研工具运行环境补齐

- 环境安装：通过 WinGet 安装并验证 Node.js 22.23.2、npm 10.9.8、Git for Windows 2.55.0 与 uv 0.12.16；Git Bash 位于 `C:\Program Files\Git\bin\bash.exe`。
- 问题定位：`src/agent-capabilities.ts` 原先固定调用 Unix `which`，Windows 没有该命令，导致已经安装的 Node/uv 仍被 Runner 误报为缺失。
- 代码修改：Windows 改用 `where.exe`，Linux/macOS 继续使用 `which`；多行查找结果只采用第一条有效路径。新增 `tests/agent-capabilities.test.ts` 防止 Node 检测回归；格式检查脚本改为由当前 Node 执行 Prettier 跨平台入口，修复 Windows `ENOENT`。
- 验证结果：专项测试 1 项通过，后台 TypeScript 构建、格式检查、文档一致性检查与 Git 差异检查通过；实际能力预检识别到 `node` 和 `uv`，Node 解析到系统安装的 22.23.2。Git Bash 标准路径存在并可执行。
- 服务验收：使用刷新后的系统 PATH 完成后台、Web 和 Agent Runner 三部分构建，服务以系统 Node.js 22.23.2 启动，`/api/health` 返回 healthy。
- 当前边界：本次补齐的是 Host Agent 的基础脚本环境，没有实现论文检索、代码定位、配置比较或日志解析，也没有安装遥感分割实验所需的 Python/PyTorch/CUDA 依赖。`feishu-cli` 与 `agent-browser` 仍为可选能力，不影响当前科研最小闭环准备。
- 下一步：准备固定版本的遥感分割仓库、论文、两份配置、匹配训练日志和一个人工核对问题，随后先实现统一证据记录。

## 2026-09-19 V0.3 科研材料目录初始审计（功能版本不变）

- 审计位置：`E:/Agent/miniclaw-data`；共 817 个文件、约 262 MB，包含 BDGI-Net 和 HBGNet、REAUNet、SEANet、BsiNet 四个对比网络。
- 材料盘点：119 个 Python 文件、81 份 YAML、75 份 LOG、234 份 CSV、6 篇 PDF；6 篇 PDF 均可提取正文，适合首版论文检索。
- 证据完整性：`snapshot-manifest.jsonl` 有 810 条唯一记录，清单所列文件全部存在，SHA-256 全部一致；目录没有检测到常见私钥、API Key 或密码配置。
- 清单缺口：6 篇 PDF 没有写入 manifest；目录不是 Git 仓库，未记录远程源码 commit。现有文件哈希可证明本地快照未变化，但不能代替远程仓库版本证明。
- 实验闭环：配置与训练 loss 日志可按目录和文件名关联，适合测试配置比较和日志解析；现有日志未包含 IoU、F1、Dice 等实际评测结果，不能据此判断论文性能结论。
- 可比性风险：抽查的主模型/no_GMRM 配置同时存在 batch size 和输入数据路径差异；Agent 必须报告这些混杂项，不能把 loss 差异直接归因于单一模块。
- 仍需补充：BDGI-Net 对应论文或方法说明、一个明确研究问题、人工核对答案、数据集/划分说明、指标口径与实际评测结果、BDGI-Net 环境依赖，以及远程 commit 或明确的 snapshot ID。
- 当前结论：材料满足静态 Tool Calling 首轮开发，不满足真实复现和论文性能结论验收。此次仅更新材料状态，`LOCAL_VERSION` 保持 0.2.1。
- 保存规范：新增 `docs/V0.3-科研材料补充与保存规范.md`，明确中央入口、独立元数据文件、完整缺口、字段模板、材料闭环、功能实现与远程只读核对顺序。该文档只定义待补内容，不把模板字段视为真实材料。

## 2026-09-19 V0.3 BDGI-Net 证据材料闭环（功能版本不变）

- 本次完成：将 BDGI-Net 在投论文纳入工作区，结构化记录表 1、2、3、4、7；建立论文、结果、三类指标、两个 Prediction Run、Ownership 和论文—源码映射。
- 掩码材料：从远程只读复制山东 73 组、四川 20 组输入—标签—保存预测，共 279 个文件、约 95 MB；不复制边界标签、边界预测或 TFNet 材料。
- 版本与完整性：工作区快照固定为 SNAP-20260919-92afd6116c4d；重新生成 snapshot-manifest.jsonl，共 1105 条唯一记录。279 个样本逐文件对比远端/本地 SHA-256，结果 279/279 一致；8 份 YAML 均可解析，未发现常见私钥或 API Key 格式。
- 论文口径：最终指标只引用 PAPER-BDGI-RESULTS.yaml；保存预测用于样例核对，不覆盖论文结果。mask_ARF.py 负责逐图像素指标，mask_eval.py 负责对象指标，不再视为冲突。
- 范围收缩：不再要求训练/权重/数据划分复现、边界预测、TFNet、训练环境或许可元数据；无法确认的保存预测生成参数保留 unknown。
- Ownership：现有论文、远程源码和保存预测属于既有研究材料；本轮完成的是材料整理、哈希核验、结构化元数据和证据映射。MiniClaw 科研工具尚未实现。
- 版本说明：本轮没有修改产品功能，LOCAL_VERSION 保持 0.2.1。
- 下一步：实现只读 manifest/元数据加载器和统一证据引用，随后接入论文检索与代码定位，先验收“GMRM 在论文中如何描述、在源码哪里定义和调用”。

## V0.3.0 — 2026-09-19 Research Workspace 状态工具客户端验收完成

- 本次实现：新增只读 Research Workspace Loader 和 research_workspace_status 工具，复用现有 Pi Runtime 工具链；普通工作区不注册该工具。
- 校验能力：覆盖 schema、相对根目录、manifest JSONL、重复路径、文件存在与大小、符号链接/父目录越界、控制文件 SHA-256、论文 YAML、Prediction Run YAML 与 snapshot 一致性。
- 输出边界：只返回相对路径和有界问题列表，不暴露宿主机绝对路径、SSH 地址或密钥；问题最多展示 50 条，但保留总数和 error 状态。
- 材料修正：miniclaw-data 入口 root 改为相对的点目录；README 明确 279 个 TIFF 只保存在本地，GitHub 克隆缺失时工具返回 degraded；同步更新 manifest 哈希。
- 依赖与运行：Runner 增加 yaml 依赖并同步 npm/pnpm 锁；挂载白名单加入 E:/Agent/miniclaw-test/miniclaw-data。首次启动发现 pnpm frozen-lockfile 不一致，补齐 Runner pnpm 锁后重新启动成功。
- 自动验证：新增 11 项专项测试，覆盖正常、旧绝对路径、重复路径、缺文件、哈希变化、路径穿越、损坏 JSONL、manifest 与材料符号链接越界、损坏 YAML、普通工作区不注册和 Runner 注册；连同既有 Runner 初始化集成用例共 18 项通过。
- 构建与真实材料：Runner 构建、Backend typecheck、Web 构建通过；构建后 Loader 对本地材料返回 ready，manifest 1105/1105、控制哈希 10/10、PAPER-BDGI 与两个 Prediction Run 正确；服务 /api/health 返回 healthy。
- 客户端验收：19:55 在宿主机工作区 BDGI-test 中发送限定验收消息；Session JSONL 记录真实 `mcp__miniclaw__research_workspace_status` Tool Call、对应 Tool Result 和最终回复。结果为 ready、workspaceId=bdgi-research、snapshotId=SNAP-20260919-92afd6116c4d、manifest 1105/1105、控制哈希 10/10、PAPER-BDGI、两个 Prediction Run、issues=[]。
- 版本说明：自动测试、真实材料检查和客户端真实 Tool Calling 均通过，LOCAL_VERSION 更新为 0.3.0。
- 下一步：实现 research_paper_search，再实现 research_code_lookup，随后验收首个“论文定义→源码位置”跨工具研究任务。

## V0.4.0 — 2026-09-19 论文按页检索客户端验收完成

- 本次实现：新增只读 `research_paper_search`，与 `research_workspace_status` 一同通过现有 Pi Runtime MCP 工具链注册；普通工作区不注册科研工具。
- 输入与输出：支持 query、paper_id 和 max_results；返回工作区/快照、检索论文、页数、匹配词、有限长度摘录、PDF 页码、相对路径和来源 SHA-256。
- 证据与安全：只检索已登记且哈希一致的 PDF；拒绝绝对路径、父目录跳转及符号链接越界；不暴露宿主机绝对路径；无匹配时返回 no_matches，不生成替代事实。
- Runtime：调用 `pdftotext -layout`，设置 20 秒超时、20 MB 输出上限和最多 10 条结果；宿主机与 Docker 镜像均已具备 Poppler/pdftotext。
- 自动验证：新增 6 项论文检索用例；连同 Workspace Loader 与 Runner 初始化集成测试共 24 项通过，Runner 构建、Backend typecheck、Web 构建、文档检查、格式检查和 Git 差异检查通过。
- 真实材料：对 PAPER-BDGI 的 16 页 PDF 搜索 `GMRM guidance-by-constraint` 返回 ok；第 5、10、11 页进入前三条，第 5 页摘录包含 GMRM 的 guidance-by-constraint 方法说明；相对来源、SHA-256 与 issues=[] 均正确。
- 客户端验收：20:49 在宿主机工作区 BDGI-test 的新对话中发送限定验收消息；Session JSONL 记录真实 `mcp__miniclaw__research_paper_search` Tool Call，参数为 PAPER-BDGI、查询 `GMRM guidance-by-constraint`、最多 3 条，并记录同一 toolCallId 的 Tool Result、`isError=false` 和最终回复。结果未调用 bash/read/grep，也未暴露宿主机绝对路径。
- 验收证据：本地运行数据中的 Session 文件记录 Tool Call ID `call_54f212a004d042f0b785ec48`；Tool Result 为 status=ok、searchedPages=16、匹配第 5/10/11 页、来源 `PDFpaper/BDGI-Net.pdf`、哈希 `4c8347ef0df8d50cf49da00a8ad847e9e04084c5fcd6478bbb1c7937231725fb`、issues=[]。运行数据不提交 Git。
- 当前限制：依赖 PDF 文字层，不做 OCR；多栏文本可能交错；当前为关键词/短语检索，不声称向量 RAG 或语义检索。
- 版本说明：实现、自动测试、真实材料检查和客户端真实 Tool Calling 均通过，LOCAL_VERSION 更新为 0.4.0。
- 下一步：实现 `research_code_lookup`，随后验收首个“论文定义→源码位置”的跨工具研究任务。

## V0.5.0 — 2026-09-19 源码定位客户端验收完成

- 本次实现：新增只读 `research_code_lookup`，复用 Research Workspace 与 Pi Runtime MCP 工具链；仅在当前快照 manifest 登记为 `source_code` 且位于模型 code 目录内的源码中检索。
- 输入与输出：支持 query、model 和 max_results；返回模型、定义/引用、最近符号、匹配行、有限上下文、相对路径、snapshotId 和 SHA-256。
- 证据与安全：逐文件核对大小与 SHA-256；拒绝绝对路径、父目录跳转和符号链接越界；2 MB 单文件读取上限、最多 20 条结果；不输出宿主机或远程绝对路径。
- 排序处理：正式 `model.py` 优先于 `model_test.py`、备份和历史辅助文件，但所有结果继续携带各自路径与哈希，不隐藏快照中的消融或历史源码。
- 自动验证：新增 5 项源码定位用例，覆盖带行号定义、无匹配、未知模型、源码哈希变化和配置越界；连同 Workspace Loader、论文检索及 Runner 初始化集成测试共 29 项通过。Runner 构建和 Backend typecheck 通过。
- 真实材料：在 BDGI-Net 的 37 个登记源码文件中，查询 `GMRM MaskGuidedByBoundaryModule` 优先返回 `mymodel/code/model.py:332`；查询 `mask_fuse` 返回第 443 行构造和第 500 行 forward 调用，快照为 `SNAP-20260919-92afd6116c4d`，源码哈希为 `1fb28fb87b3f846b9d2f7743eb0ec291fbabe73126bbf4aba835031a4915a39d`，issues=[]。
- 工具链验证：通过 `createResearchWorkspaceTools` 取得已注册的 `research_code_lookup` handler 并直接调用，返回 status=ok、正式 model.py 的构造/调用证据和相同 snapshot/SHA-256。
- 当前限制：文本与符号定位不能代替 AST、调用图或运行证明；复杂动态绑定仍需人工核对。本阶段不执行源码、不加载模型权重。
- 客户端验收：23:08 在宿主机工作区 BDGI-test 中发送限定验收消息；Session JSONL 记录同一模型回合内两次真实 `mcp__miniclaw__research_code_lookup` Tool Call，参数分别为 `GMRM MaskGuidedByBoundaryModule` 与 `mask_fuse`，并记录两个对应 Tool Result、`isError=false` 和最终回复。
- 客户端结果：两次结果均为 status=ok、searchedFiles=37、issues=[]；正式 `mymodel/code/model.py` 返回 GMRM 类定义第 332 行、构造第 443 行和 forward 调用第 500 行，snapshotId 与 SHA-256 一致。Tool Call ID 分别为 `call_b7ceeba59ede48129156b0b3` 和 `call_98e232d8d5364d33ae0a7a75`；运行数据不提交 Git。
- 证据边界：当前摘录只返回最近函数符号，未包含构造与 forward 的外层类定义；Agent 明确说明不能仅凭该 Tool Result 确认外层类名，没有把未返回信息写成事实。后续跨工具任务可额外检索 `Tripmodel` 完成闭环。
- 版本说明：实现、自动测试、真实材料、已注册 handler 直接调用和客户端真实 Tool Calling 均通过，LOCAL_VERSION 更新为 0.5.0。
- 下一步：执行首个“论文定义→源码位置”跨工具任务，同时检索论文 GMRM 定义、`Tripmodel` 类和 `mask_fuse` 构造/调用；通过后再实现配置比较工具。

## V0.5.1 开发中 — 2026-09-19 科研回答与结构化结果修复

- 问题记录：HBGNet 回答虽然检索到论文表格，但自然语言阶段把 REAUNet 的 142.40M/2035.05G 错写成 BDGI-Net；正确值分别为 HBGNet 30.45M/463.81G、BDGI-Net 20.09M/321.98G、REAUNet 142.40M/2035.05G。根因是从 PDF 表格片段组织数值时缺少行级实体绑定。
- 用户体验：历史回答默认展示工具名、status、workspaceId、snapshotId 和 SHA-256，证据层信息过度暴露，影响普通用户阅读。
- 代码修改：新增 `research_result_lookup`，从论文结构化结果读取数据集数量、损失权重、三场景对比、复杂度和消融结果；模型、场景、指标与数值按原表整行绑定。
- 回答策略：科研工作区注入专用证据规范；普通问题默认自然语言回答，引用简化为论文表格/页码或源码文件/行号。论文正文工具不再用于提取定量表格结论。
- 证据分层：论文检索、源码定位和结构化结果工具默认不返回快照与哈希；显式审计参数 `include_internal_evidence=true` 时保留内部证据能力。
- 自动验证：Runner 构建通过；科研工具、Prompt 组装、Prompt 顺序及 Prompt 文件加载共 42 项测试通过。新增用例确认相邻模型行不会混合，默认输出无快照/哈希，审计模式仍可取得内部证据。
- 真实材料检查：对当前 miniclaw-data 查询 HBGNet 复杂度返回 30.45M/463.81G，来源为论文表 4（PDF 第 9 页），默认结果未包含 snapshotId 或 SHA-256。
- 当前状态：实现和自动验证完成，客户端真实模型验收待进行；`LOCAL_VERSION` 暂保持 0.5.0。详细说明见 `docs/V0.5.1-科研回答与结构化结果修复说明.md`。
- 下一步：重新构建并启动服务，在 BDGI-test 新对话中同时询问 HBGNet 的复杂度和山东指标；确认自然语言、数值、来源与默认脱敏均符合预期后，将版本更新为 0.5.1。

## V0.6.0 — 2026-09-20 通用论文分析客户端验收完成

- 产品范围：继续采用个人科研工作区模式，本阶段不增加多人共享项目和成员权限。
- 本次实现：新增只读 `research_paper_analyze`。支持已登记 `paper_id` 和工作区内相对 `paper_path`；只有 PDF 时也可分析，不要求先准备源码、配置、日志或结构化结果。
- 论文能力：识别摘要、引言、相关工作、方法、实验、结果、局限与结论章节，返回有限摘录和页码；同时给出当前能否总结论文、解释方法、分析论文内部实验、核验源码、核验运行和复现实验。
- 缺口处理：分别记录源码、结构化结果、配置、日志、数据和权重缺失的影响。只有论文时仍完成正文分析，并明确论文陈述不等于独立复现。
- 安全与证据：只允许工作区内相对 PDF 路径；拒绝绝对路径、父目录跳转、工作区外符号链接和超过 100 MB 的文件；普通输出隐藏快照与 SHA-256，审计参数开启后保留内部证据。
- 失败处理：文字层不足返回 `OCR_REQUIRED`，不根据文件名推断论文内容；没有标准章节标题时返回文档开头并标记部分完成。
- 自动验证：新增 7 项通用论文测试，覆盖未登记 PDF、已登记论文关联材料、无实验论文、OCR 缺口、路径越界、输入冲突和默认脱敏；科研及 Prompt 相关 49 项测试通过，Runner 构建和后台类型检查通过。
- 真实材料：使用未登记的 `PDFpaper/UNet.pdf` 验证，返回 8 页、`paper_only`，识别第 1 页摘要/引言、第 4 页 Network Architecture、第 6 页 Experiments 和第 7 页 Conclusion；未把正文 “result by...” 误判为章节标题。
- 客户端验收：在 `BDGI-test` 中直接发送自然语言问题，未指定工具名；后台 Session 记录确认自动调用 `mcp__miniclaw__research_paper_analyze`，参数为 `paper_path=PDFpaper/UNet.pdf`，Tool Result 成功后生成自然语言回答。
- 展示验收：回答覆盖研究问题、方法、实验、结论和可验证边界，并附页码；未展示 JSON、工具状态、snapshotId 或 SHA-256。普通输出进一步移除 `registration`，将未登记状态统一解释为“当前只提供论文 PDF”。
- 当前状态：实现、真实 PDF、自动工具选择和客户端真实模型 Tool Calling 均已通过，`LOCAL_VERSION` 更新为 0.6.0。使用说明见 `docs/V0.6-通用论文分析工具使用说明.md`。
- 下一步：实现论文登记与 Claim–Evidence 持久化，使新论文分析结果能够保存为研究记录，并支持后续会话继续核验。
